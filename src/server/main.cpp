#include <iostream>
#include "server.h"
#include "public.hpp"
int main(){
    // EventLoop loop;
    // InetAddress addr("127.0.0.1",8080);
    // ChatServer server(&loop,addr,"chatserver");
    // server.start();
    // loop.loop();
    EnMsgType msg = REG_MSG;
    msg = REG_MSG;
    std::cout<<msg<<"\n";
    std::cout<<"hello\n";
    return 0;
}